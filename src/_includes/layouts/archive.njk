{% extends "layouts/post.njk" %}
{% block postContent %}
    {% for post in collections.posts | filterByTag(tagFilter) %}
        <article class="container mx-auto">
            <a href="{{ post.url }}">{{ post.data.title }}</a>
        </article>
    {% endfor %}
{% endblock %}